# The scraper uses Python's standard library to read the public PvP API.
# Pytest protects the published aggregation rules in GitHub Actions. Pin the
# version used in CI so a new major/minor resolver result cannot silently alter
# the release gate; Dependabot proposes reviewed updates.
pytest==9.1.1
